# UPDATE reports the number of rows changed and the change is visible to later queries.

statement ok
CREATE TABLE t(a INT, b INT);

statement ok
INSERT INTO t VALUES (1,1),(2,2),(3,3);

query
UPDATE t SET b = 0 WHERE a >= 2;
----
2

query rowsort
SELECT a, b FROM t;
----
1 1
2 0
3 0
