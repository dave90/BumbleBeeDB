[pytest]
# Discover .slt files as tests (see conftest.py's pytest_collect_file). `python_files` is
# irrelevant here since there are no test_*.py modules, but testpaths keeps discovery scoped
# to the corpus directory.
testpaths = slt
addopts = -ra
